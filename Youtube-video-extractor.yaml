services:
  - type: web
    name: youtube-video-extractor-backend
    runtime: node
    rootDir: server
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: OPENAI_API_KEY
        sync: false  # You need to set this manually
      - key: NODE_ENV
        value: production